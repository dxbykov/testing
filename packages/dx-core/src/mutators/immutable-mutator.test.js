import { immutableMutator } from './immutable-mutator';

describe('immutableMutator', () => {
    
    //TODO

    test('#set', () => {
        expect(immutableMutator.set).not.toBeUndefined();
    });

    test('#splice', () => {
        expect(immutableMutator.splice).not.toBeUndefined();
    });

});
